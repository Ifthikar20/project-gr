import DesignSystem
import SwiftUI

/// Value-prop pager + permission priming + Sign in with Apple (docs/03 §1).
/// Phase A stub; built for real in Phase E (E5).
public struct OnboardingView: View {
    public init() {}

    public var body: some View {
        PlaceholderScreen(
            title: "GemRun",
            subtitle: "Routes are treasure maps. Run to collect.",
            systemImage: "sparkles"
        )
    }
}
